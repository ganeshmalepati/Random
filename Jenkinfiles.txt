pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                // Clone your GitHub repository
                git 'https://github.com/ganeshmalepati/Docker_images_Practice.git'
            }
        }
        stage('Build Docker Image') {
            steps {
                script {
                    // Build your Docker image
                    sh 'docker build -t rand.py .'
                }
            }
        }
/*
        stage('Run Docker Container') {
            steps {
                script {
                    // Run your Docker container
                    sh 'docker run -d --name your-container-name your-image-name'
                }
            }
        }
*/
    }

    post {
        always {
            // Cleanup if needed
            sh 'docker stop your-container-name || true'
            sh 'docker rm your-container-name || true'
        }
    }
}
